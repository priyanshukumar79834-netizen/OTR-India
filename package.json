{
  "name": "otr-india-anchal-module",
  "version": "0.1.0",
  "description": "OTR-India — Consent + Document/Data Management + Application Flow module (Anchal). Standalone/independent prototype: in-memory store, ready to be wired into the shared repo/schema.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^4.19.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
